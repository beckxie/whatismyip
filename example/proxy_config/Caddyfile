foo.example.com {
    # Caddy v2 log configuration
    log {
        output file access_log.log
    }

    # Caddy v2 compression
    encode gzip

    # Caddy v2 reverse proxy
    # Note: Caddy v2 automatically sets X-Forwarded-For and X-Forwarded-Proto headers
    reverse_proxy 127.0.0.1:9999
}
